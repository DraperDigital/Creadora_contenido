# Remotion tracks the registry "latest" tag (see package.json); a lockfile
# would pin it again, so npm must never create or read one here. The file is
# also git-ignored (root .gitignore + pipeline/.gitignore).
package-lock=false
