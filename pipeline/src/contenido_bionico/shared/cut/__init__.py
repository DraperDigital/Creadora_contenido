"""Shared cut-phase implementation for the long and short pipelines.

`long/cut/` and `short/cut/` are thin shims that forward here; the two
pipelines run the exact same cut flow (the agent prompts in `agents/` are
also shared).
"""
